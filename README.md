# Smart Resource Allocation & Rapid Crisis Response

React 18 + Vite frontend prototype with Tailwind CSS, Framer Motion, React Three Fiber/Drei, React Router DOM, and Lucide React.

## Project Location

This project is on your PC here:

`C:\Users\acer\Documents\Codex\2026-04-20-act-as-an-expert-frontend-react`

## Required Downloads

Install these on your PC:

1. Node.js LTS
   Download: https://nodejs.org/
   Recommended: Node.js 20 LTS or newer.
   npm is included with Node.js.

2. Visual Studio Code
   Download: https://code.visualstudio.com/
   Optional, but recommended for editing.

3. Git for Windows
   Download: https://git-scm.com/download/win
   Optional, but useful if you want version control.

## Tech Stack Used

- React 18
- Vite
- Tailwind CSS
- Framer Motion
- React Three Fiber
- Drei
- React Router DOM
- Lucide React
- Three.js

## Run In CMD

Open `cmd` and run:

```bat
cd /d C:\Users\acer\Documents\Codex\2026-04-20-act-as-an-expert-frontend-react
npm install
npm run dev
```

If `npm run dev` does not work in PowerShell, use Command Prompt (`cmd`) instead.

Then open:

`http://localhost:5173/`

If port `5173` is busy, Vite may use `5174` or another nearby port.

## Build For Production

```bat
cd /d C:\Users\acer\Documents\Codex\2026-04-20-act-as-an-expert-frontend-react
npm run build
```

Production files will be generated in:

`C:\Users\acer\Documents\Codex\2026-04-20-act-as-an-expert-frontend-react\dist`

## Helpful Windows Scripts

- `run-project.cmd` starts the dev server in `cmd`
- `build-project.cmd` builds the project in `cmd`

